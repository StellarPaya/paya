[package]
name = "atomic_swap_contract"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
soroban-sdk = { workspace = true }
sha2 = { version = "0.10", optional = true }

[features]
testutils = ["soroban-sdk/testutils"]